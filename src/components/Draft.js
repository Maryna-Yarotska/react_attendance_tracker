// const [showInput, setShowInput] = useState(false);
 
 {/* <button
        type="submit"
        className="btn"
        onClick={() => {
          setShowInput(!showInput);
        }}
      >
        Milada Timonova
      </button>
      {showInput && <InputForm />} */}

      {/* second group */}
      {/* <button type="submit" className="btn">
        Max Json
      </button> */}


      

      
        
        
        
        //find parentId
        //const [parent, setp'arent] = us'eState(second)
  // const findParentId = date.find((i) => i.id == selectedGroup);
  
  
    // function selectingGroup(e) {
    //   const group = date.find((i) => i.id == e.target.value);
    //   setSelectedGroup(group);
    // }
    // const findId = date.find((i) => i.id == selectedGroup);}
  
    // {selectedGroup.children.map((i) => (
    //   <button type="submit" className="btn" onClick={handleClick}>
    //     {i.name}
    //   </button>

  

    //SELECTED NAMES

  // function selectingNames(e) {
  //   const name = date.find(i => i.children == e.target.value)
  //   setSelectedNames(name)

  // const findReason = date.find((i) => i.children == selectedNames);

      // {selectedNames.children.map((i) =>(<p>{i.absence.reason}</p>))}

      // {selectedNames.children.map((i) =>(<p>{i.absence.date}</p>))}

    //   </div>

    //   {selectedGroup.children.map((i) => (
    //     <button
    //       value={i.childId}
    //       type="submit"
    //       className="btn"
    //       onClick={handleClick}
    //     >
    //       {i.name}
    //     </button>
    //   ))}
    // </div>
     


